<%- include("../index.ejs") %>

<h2><%= title %></h2>
<div><a href="/artists/create">Add</a></div>
<ul>
  <% if(artists.length > 0 ) { %>
    <% for(const artist of artists) {%>
        <li><a href="<%= artist.url%>"><%= artist.name%></a></li>
    <% } %>
  <% } else { %>
    <p>There's no artists yet.</p>
    <% } %>
</ul>