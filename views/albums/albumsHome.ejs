<%- include('../index.ejs') %>
<div><a href="/albums/create">Add</a></div>
<ul>
    <% if(albums.length > 0 ) { %>
      <% for(const album of albums) {%>
          <li><a href="<%= album.url%>"><%= album.title%></a> by <a href="<%= album.artist.url%>"><%= album.artist.name%></a></li>
      <% } %>
    <% } else { %>
      <p>There's no albums yet.</p>
      <% } %>
  </ul>